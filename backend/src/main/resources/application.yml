spring:
  application:
    name: quickcart-backend
  datasource:
    url: jdbc:postgresql://${DB_HOST:database-1.cu3ikoym0hs3.us-east-1.rds.amazonaws.com}:${DB_PORT:5432}/${DB_NAME:studentdb}
    username: ${DB_USER:admin123}
    password: ${DB_PASSWORD:sunny@2005}
    driver-class-name: org.postgresql.Driver
  # We use ddl-auto: update based on deployment settings.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: never


jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expirationMs: 86400000 # 24 hours

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

aws:
  s3:
    bucket: ${S3_BUCKET_NAME:quickcart-media}
    region: ${AWS_REGION:ap-south-1}

ai:
  groq:
    api-key: ${GROQ_API_KEY:}
  nvidia:
    api-key: ${NVIDIA_API_KEY:}
  gemini:
    api-key: ${GEMINI_API_KEY:}

